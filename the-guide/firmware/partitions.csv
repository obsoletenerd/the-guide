# Name,   Type, SubType,  Offset,   Size,     Flags
# Single-app table, no OTA (see PLAN.md #3: "no OTA to start"). 16 MB flash;
# app gets 15 MB (room for the reader + fonts + zstd + full builtin fonts),
# a coredump partition for crash diagnostics, and ~0.9 MB left spare.
nvs,      data, nvs,      0x9000,   0x5000,
factory,  app,  factory,  0x10000,  0xF00000,
coredump, data, coredump, 0xF10000, 0x10000,
